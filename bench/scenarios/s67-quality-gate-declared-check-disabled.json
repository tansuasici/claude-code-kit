{
  "name": "s67-quality-gate-declared-check-disabled",
  "hook": ".claude/hooks/quality-gate.sh",
  "setup_files": {
    ".claude/commands.json": "{\"lint\": \"\"}",
    "src/x.py": "x = 1\n"
  },
  "payload": { "tool_name": "Edit", "tool_input": { "file_path": "{TMPROOT}/src/x.py" } },
  "expect": {
    "exit_code": 0,
    "stdout_contains": ["NOT verified", "disabled"],
    "state": [
      { "file": ".hook-state/last_quality_gate.json", "field": "status", "equals": "skipped" },
      { "file": ".hook-state/last_quality_gate.json", "field": "tool", "equals": "" }
    ]
  },
  "notes": "lint set to \"\" means the project has no lint. The edit is recorded as skipped (disabled) — not silently checked with a guessed tool (py_compile) and passed as if the project's own check ran."
}
