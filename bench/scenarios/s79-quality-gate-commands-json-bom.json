{
  "name": "s79-quality-gate-commands-json-bom",
  "hook": ".claude/hooks/quality-gate.sh",
  "setup_files": {
    ".claude/commands.json": "\ufeff{\"lint\": \"false\"}",
    "src/x.py": "x = 1\n"
  },
  "payload": { "tool_name": "Edit", "tool_input": { "file_path": "{TMPROOT}/src/x.py" } },
  "expect": {
    "exit_code": 0,
    "stdout_not_contains": ["BOM"],
    "state": [
      { "file": ".hook-state/last_quality_gate.json", "field": "status", "equals": "failed" },
      { "file": ".hook-state/last_quality_gate.json", "field": "tool", "equals": "false" }
    ]
  },
  "notes": "A commands.json saved with a UTF-8 byte-order mark was rejected (\"Unexpected UTF-8 BOM\") and blocked every code edit. It is now read as utf-8-sig: the declared lint runs (here `false`, so the edit fails as declared)."
}
