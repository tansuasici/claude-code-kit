# Tech debt tracker

> _Stub generated by `/harness-init`. Append known debt as it surfaces — don't try to backfill exhaustively._

## Standing items

| Area | Description | Cost if unfixed | Effort to fix |
|---|---|---|---|
| _example-api_ | _Hand-rolled retry logic; should move to shared client_ | _Inconsistent retry behavior across services_ | _M_ |

## Recently resolved

| Area | Description | Resolved when / link |
|---|---|---|
| _example_ | _Replaced custom logger with shared package_ | _PR #123_ |

## How to use

- Add an entry when you notice debt mid-task (don't fix it mid-task; that's scope drift — log here and move on)
- Reference items from `exec-plans/active/*` when a plan addresses one
- Move resolved items to the "Recently resolved" table; trim that table when it grows past ~10 entries
