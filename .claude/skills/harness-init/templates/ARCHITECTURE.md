# Architecture

> _Stub generated by `/harness-init`. Fill in your actual architecture — this skeleton is a starting point, not a deliverable._

## Overview

One paragraph: what does this system do, at the highest level?

## Services / Modules

| Service | Responsibility | Owner | Repo / path |
|---|---|---|---|
| _example-api_ | _HTTP API for client apps_ | _backend team_ | _src/api/_ |

## Data flow

Describe (or diagram) how requests flow through the system end-to-end. Include the storage layer, message queues, external integrations.

## Boundaries

What's in scope for this system, and what crosses a network/process boundary? Each boundary is a place to enforce contracts (typed APIs, schemas, retries).

## External dependencies

| Service | Purpose | Docs |
|---|---|---|
| _Stripe_ | _payments_ | _stripe.com/docs_ |

## Decisions

Link to `tasks/decisions.md` ADRs that shaped this architecture, or to `docs/design-docs/` for deeper opinions.
